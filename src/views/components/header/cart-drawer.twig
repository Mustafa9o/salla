{# ============================================================ #}
{# CART DRAWER — components/header/cart-drawer.twig             #}
{# Uses Salla's built-in cart web component                     #}
{# ============================================================ #}

{# Salla handles the full cart UI including drawer via this component #}
<salla-cart></salla-cart>
